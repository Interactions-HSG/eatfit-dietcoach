FROM python:2.7-jessie
RUN apt-get update && apt-get install -y python-dev libmysqlclient-dev build-essential unixodbc unixodbc-dev
WORKDIR "/app"
COPY . .
RUN pip install -r requirements.txt
CMD sh worker.sh